# Web Chat ERPClaw — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aggiungere una pagina web chat a `/chat` nell'app FastAPI esistente, che permette al titolare di interagire con l'agente AI dalla stessa sessione di memoria del bot Telegram.

**Architecture:** Nuovo `APIRouter` in `erpclaw/chat.py` con due route (GET + POST). Storia conversazione mantenuta in un dict in-memory keyed by UUID cookie. Il markdown dell'agente viene convertito in HTML con `markdown2`. Montato in `web.py` accanto allo shop router.

**Tech Stack:** FastAPI, Jinja2, HTMX 2.0.3 (CDN), Bootstrap 5.3.3 (CDN), markdown2, UUID cookie session, `run_agent()` da `erpclaw/agent.py`

**Nota su user_id:** La spec originale indicava di aggiungere `TITOLARE_TELEGRAM_ID` a `config.py` e `.env`. Non è necessario: `ALLOWED_CHAT_ID` è già presente in `config.py` ed è esattamente l'ID Telegram del titolare. Viene riusato direttamente come `user_id` per l'agente, garantendo memoria condivisa tra Telegram e web chat senza modifiche all'env.

**Nota sull'ordinamento TDD:** I test per gli helper puri (`test_chat_session.py`) vengono verificati subito dopo la creazione di `chat.py`. I test HTTP (`test_chat_routes.py`) vengono verificati solo dopo la creazione dei template, poiché `GET /chat` renderizza `Jinja2Templates` e necessita dei file template.

---

## Chunk 1: Dipendenza markdown2

### Task 1: Aggiungere markdown2 alle dipendenze

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: Aggiungere markdown2 a pyproject.toml**

In `pyproject.toml`, nella lista `dependencies`, aggiungere dopo `"jinja2>=3.1.0"`:

```toml
    "markdown2>=2.4.0",
```

- [ ] **Step 2: Installare il pacchetto**

```bash
uv pip install markdown2
```

Expected output: `Successfully installed markdown2-x.x.x`

- [ ] **Step 3: Verificare l'import**

```bash
uv run --no-sync python -c "import markdown2; print(markdown2.markdown('**ok**'))"
```

Expected output: `<p><strong>ok</strong></p>`

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml
git commit -m "feat: add markdown2 dependency for web chat"
```

---

## Chunk 2: Router chat e test session helpers

### Task 2: Creare `erpclaw/chat.py` con test degli helper puri

**Files:**
- Create: `erpclaw/chat.py`
- Create: `tests/test_chat_session.py`

- [ ] **Step 1: Scrivere test per la gestione sessione (helper puri, senza template)**

Creare `tests/test_chat_session.py`:

```python
"""Tests for web chat session management helpers."""
import uuid
from erpclaw.chat import _get_or_create_session_id, _get_history, _add_message, chat_sessions


def test_get_or_create_session_id_generates_uuid():
    """Se non c'è cookie, genera un UUID valido."""
    sid = _get_or_create_session_id(None)
    uuid.UUID(sid)  # lancia ValueError se non è UUID valido


def test_get_or_create_session_id_preserves_existing():
    """Se il cookie esiste, lo restituisce invariato."""
    existing = str(uuid.uuid4())
    assert _get_or_create_session_id(existing) == existing


def test_get_history_empty_for_new_session():
    """Sessione nuova ha storia vuota."""
    sid = str(uuid.uuid4())
    assert _get_history(sid) == []


def test_add_message_appends_entry():
    """add_message aggiunge entry con role e content."""
    sid = str(uuid.uuid4())
    _add_message(sid, "user", "ciao")
    _add_message(sid, "assistant", "risposta")
    history = _get_history(sid)
    assert len(history) == 2
    assert history[0] == {"role": "user", "content": "ciao"}
    assert history[1] == {"role": "assistant", "content": "risposta"}


def test_add_message_multiple_sessions_isolated():
    """Sessioni diverse hanno storie indipendenti."""
    sid1 = str(uuid.uuid4())
    sid2 = str(uuid.uuid4())
    _add_message(sid1, "user", "messaggio sid1")
    assert _get_history(sid2) == []
```

- [ ] **Step 2: Eseguire test per verificare che falliscano**

```bash
uv run --no-sync python -m pytest tests/test_chat_session.py -v
```

Expected: FAIL con `ModuleNotFoundError` (`erpclaw.chat` non esiste ancora)

- [ ] **Step 3: Implementare `erpclaw/chat.py`**

```python
"""Web chat interface for ERPClaw — accessible at /chat."""

import uuid
from typing import Any

import markdown2
from fastapi import APIRouter, Form, Request
from fastapi.responses import HTMLResponse
from fastapi.templating import Jinja2Templates

from erpclaw.agent import run_agent
from erpclaw.config import ALLOWED_CHAT_ID

router = APIRouter(prefix="/chat")
templates = Jinja2Templates(directory="erpclaw/templates")

COOKIE_NAME = "chat_session"

# In-memory session store: {session_id: [{"role": str, "content": str}]}
chat_sessions: dict[str, list[dict[str, Any]]] = {}


# --- Session helpers (pure functions, testable) ---

def _get_or_create_session_id(cookie_value: str | None) -> str:
    """Return existing session ID from cookie, or generate a new UUID."""
    if cookie_value:
        return cookie_value
    return str(uuid.uuid4())


def _get_history(session_id: str) -> list[dict[str, Any]]:
    """Return message history for a session (empty list if new)."""
    return chat_sessions.get(session_id, [])


def _add_message(session_id: str, role: str, content: str) -> None:
    """Append a message to the session history."""
    if session_id not in chat_sessions:
        chat_sessions[session_id] = []
    chat_sessions[session_id].append({"role": role, "content": content})


# --- Routes ---

@router.get("", response_class=HTMLResponse)
async def chat_page(request: Request):
    sid = _get_or_create_session_id(request.cookies.get(COOKIE_NAME))
    history = _get_history(sid)
    resp = templates.TemplateResponse(
        "chat/chat.html",
        {"request": request, "history": history},
    )
    resp.set_cookie(COOKIE_NAME, sid, httponly=True, samesite="lax")
    return resp


@router.post("/send", response_class=HTMLResponse)
async def chat_send(request: Request, message: str = Form(...)):
    sid = _get_or_create_session_id(request.cookies.get(COOKIE_NAME))

    _add_message(sid, "user", message)

    try:
        raw_response = await run_agent(message, user_id=ALLOWED_CHAT_ID)
        html_response = markdown2.markdown(
            raw_response or "(risposta vuota)",
            extras=["tables", "fenced-code-blocks", "strike"],
        )
        _add_message(sid, "assistant", html_response)
    except Exception as e:
        _add_message(sid, "assistant", f"<p class='text-danger'>Errore: {e}</p>")

    history = _get_history(sid)
    resp = templates.TemplateResponse(
        "chat/_messaggi.html",
        {"request": request, "history": history},
    )
    resp.set_cookie(COOKIE_NAME, sid, httponly=True, samesite="lax")
    return resp
```

- [ ] **Step 4: Eseguire i test session helpers (soli 5, no template necessari)**

```bash
uv run --no-sync python -m pytest tests/test_chat_session.py -v
```

Expected: tutti e 5 PASS

- [ ] **Step 5: Commit**

```bash
git add erpclaw/chat.py tests/test_chat_session.py
git commit -m "feat: add web chat router with session management"
```

---

## Chunk 3: Template Jinja2 e test route HTTP

### Task 3: Creare i template Jinja2

**Files:**
- Create: `erpclaw/templates/chat/chat.html`
- Create: `erpclaw/templates/chat/_messaggi.html`

- [ ] **Step 1: Creare la directory template**

```bash
mkdir -p erpclaw/templates/chat
```

- [ ] **Step 2: Creare `erpclaw/templates/chat/_messaggi.html`**

Questo è il partial restituito da HTMX dopo ogni messaggio. I messaggi `user` appaiono a destra in blu; quelli `assistant` a sinistra con HTML renderizzato (nota il filtro `| safe`).

```html
{% for msg in history %}
  {% if msg.role == "user" %}
  <div class="d-flex justify-content-end mb-2">
    <div class="bg-primary text-white rounded px-3 py-2" style="max-width:75%">
      {{ msg.content }}
    </div>
  </div>
  {% else %}
  <div class="d-flex justify-content-start mb-2">
    <div class="bg-light border rounded px-3 py-2 chat-md" style="max-width:90%">
      {{ msg.content | safe }}
    </div>
  </div>
  {% endif %}
{% endfor %}
```

- [ ] **Step 3: Creare `erpclaw/templates/chat/chat.html`**

Note architetturali:
- `hx-on::after-request` sul form gestisce solo il reset del form (non dipende dal DOM aggiornato).
- Lo scroll al fondo avviene via listener `htmx:afterSwap` su `document.body`, che scatta dopo che HTMX ha aggiornato il DOM.

```html
<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ERPClaw Chat</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://unpkg.com/htmx.org@2.0.3" defer></script>
  <style>
    #chat-box { height: calc(100vh - 180px); overflow-y: auto; }
    .chat-md table { width: 100%; border-collapse: collapse; margin: .5rem 0; }
    .chat-md th, .chat-md td { border: 1px solid #dee2e6; padding: .25rem .5rem; }
    .chat-md th { background: #f8f9fa; }
    .chat-md pre { background: #f8f9fa; padding: .5rem; border-radius: 4px; overflow-x: auto; }
    .htmx-indicator { display: none; }
    .htmx-request .htmx-indicator { display: inline-block; }
  </style>
</head>
<body>
  <nav class="navbar navbar-dark bg-dark mb-0">
    <div class="container-fluid">
      <span class="navbar-brand mb-0 h1">ERPClaw Chat</span>
      <a href="/admin" class="text-light small">Admin →</a>
    </div>
  </nav>

  <div class="container-fluid px-3 pt-3 pb-0" style="max-width: 900px">
    <div id="chat-box" class="border rounded p-3 bg-white mb-2">
      {% include "chat/_messaggi.html" %}
    </div>

    <form id="chat-form"
          hx-post="/chat/send"
          hx-target="#chat-box"
          hx-swap="innerHTML"
          hx-indicator="#spinner"
          hx-on::after-request="this.reset(); this.querySelector('input').focus();"
          class="d-flex gap-2">
      <input type="text" name="message" class="form-control"
             placeholder="Scrivi un messaggio..." autocomplete="off" required autofocus>
      <button type="submit" class="btn btn-primary px-4">
        <span id="spinner" class="htmx-indicator spinner-border spinner-border-sm me-1"></span>
        Invia
      </button>
    </form>
  </div>

  <script>
    // Scroll al fondo dopo ogni swap HTMX (dopo che il DOM è aggiornato)
    document.body.addEventListener('htmx:afterSwap', function(e) {
      if (e.detail.target.id === 'chat-box') {
        e.detail.target.scrollTop = e.detail.target.scrollHeight;
      }
    });
    // Scroll iniziale al fondo se ci sono messaggi da sessioni precedenti
    window.addEventListener('load', function() {
      var box = document.getElementById('chat-box');
      box.scrollTop = box.scrollHeight;
    });
  </script>
</body>
</html>
```

- [ ] **Step 4: Verificare che i template siano sintatticamente corretti**

```bash
uv run --no-sync python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('erpclaw/templates'))
env.get_template('chat/chat.html')
env.get_template('chat/_messaggi.html')
print('Templates OK')
"
```

Expected: `Templates OK`

- [ ] **Step 5: Commit**

```bash
git add erpclaw/templates/chat/
git commit -m "feat: add web chat Jinja2 templates"
```

### Task 4: Scrivere e verificare i test route HTTP

**Files:**
- Create: `tests/test_chat_routes.py`

I test HTTP richiedono i template già presenti (creati al Task 3). Vengono eseguiti dopo la creazione dei template.

- [ ] **Step 1: Creare `tests/test_chat_routes.py`**

```python
"""Tests for web chat HTTP routes.

NOTE: questi test richiedono che i template esistano in erpclaw/templates/chat/
Eseguire solo dopo aver creato i template (Task 3).
I test vengono eseguiti dal project root dove erpclaw/templates/ è visibile.
"""
import pytest
from unittest.mock import AsyncMock, patch
from fastapi.testclient import TestClient
from fastapi import FastAPI
from erpclaw.chat import router, chat_sessions


@pytest.fixture(autouse=True)
def clear_sessions():
    """Pulisce lo stato delle sessioni tra i test."""
    chat_sessions.clear()
    yield
    chat_sessions.clear()


@pytest.fixture
def client():
    app = FastAPI()
    app.include_router(router)
    return TestClient(app, raise_server_exceptions=True)


def test_get_chat_returns_200(client):
    """GET /chat restituisce 200 e imposta il cookie sessione."""
    response = client.get("/chat")
    assert response.status_code == 200
    assert "chat_session" in response.cookies


def test_get_chat_contains_form(client):
    """GET /chat contiene il form di invio messaggio."""
    response = client.get("/chat")
    assert b'name="message"' in response.content


def test_post_send_returns_html_with_user_message(client):
    """POST /chat/send include il messaggio utente nella risposta."""
    with patch("erpclaw.chat.run_agent", new=AsyncMock(return_value="risposta agente")):
        response = client.post("/chat/send", data={"message": "ciao"})
    assert response.status_code == 200
    assert b"ciao" in response.content


def test_post_send_returns_html_with_agent_response(client):
    """POST /chat/send include la risposta dell'agente nella risposta."""
    with patch("erpclaw.chat.run_agent", new=AsyncMock(return_value="**risposta**")):
        response = client.post("/chat/send", data={"message": "test"})
    assert response.status_code == 200
    # markdown2 converte **risposta** in <strong>risposta</strong>
    assert b"risposta" in response.content


def test_post_send_handles_none_response(client):
    """POST /chat/send gestisce risposta None senza crash."""
    with patch("erpclaw.chat.run_agent", new=AsyncMock(return_value=None)):
        response = client.post("/chat/send", data={"message": "test"})
    assert response.status_code == 200


def test_post_send_handles_agent_exception(client):
    """POST /chat/send mostra errore inline se l'agente lancia eccezione."""
    with patch("erpclaw.chat.run_agent", new=AsyncMock(side_effect=Exception("errore test"))):
        response = client.post("/chat/send", data={"message": "test"})
    assert response.status_code == 200
    assert b"Errore" in response.content
```

- [ ] **Step 2: Eseguire i test route (i template esistono ora)**

```bash
uv run --no-sync python -m pytest tests/test_chat_routes.py -v
```

Expected: tutti e 6 i test PASS

- [ ] **Step 3: Commit**

```bash
git add tests/test_chat_routes.py
git commit -m "test: add HTTP route tests for web chat"
```

---

## Chunk 4: Integrazione in web.py e verifica finale

### Task 5: Montare il router in web.py

**Files:**
- Modify: `erpclaw/web.py`

- [ ] **Step 1: Aggiungere l'import e il mount del router**

In `erpclaw/web.py`, dopo la riga `from erpclaw.shop import router as shop_router`, aggiungere:

```python
from erpclaw.chat import router as chat_router
```

E dopo `app.include_router(shop_router)`, aggiungere:

```python
app.include_router(chat_router)
```

- [ ] **Step 2: Verificare che l'app abbia le route giuste (richiede .env presente)**

```bash
uv run --no-sync python -c "
from erpclaw.web import app
paths = [r.path for r in app.routes if hasattr(r, 'path')]
assert '/chat' in paths, f'/chat non trovato in {paths}'
assert '/chat/send' in paths, f'/chat/send non trovato in {paths}'
print('Routes OK:', paths)
"
```

Expected: stampa lista route che include `/chat` e `/chat/send`

- [ ] **Step 3: Eseguire la suite completa**

```bash
uv run --no-sync python -m pytest tests/ -v
```

Expected: tutti i test esistenti + 11 test chat (5 session + 6 route) PASS, zero failure

- [ ] **Step 4: Smoke test manuale**

Avviare il server (richiede `.env` con tutte le variabili):

```bash
uv run uvicorn erpclaw.web:app --reload
```

Aprire `http://localhost:8000/chat` e verificare:
- La pagina si carica con navbar e form
- Inviando un messaggio: il messaggio utente appare a destra in blu
- Lo spinner appare durante l'attesa della risposta agente
- La risposta dell'agente appare a sinistra con tabelle markdown formattate
- Il form si svuota dopo l'invio
- Lo scroll porta automaticamente all'ultimo messaggio

- [ ] **Step 5: Commit e push**

```bash
git add erpclaw/web.py
git commit -m "feat: mount web chat router in FastAPI app"
git push origin master
```
